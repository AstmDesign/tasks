<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :task_id %>
    <%= form.select :task_id, options_from_collection_for_select(@tasks, :id, :title, comment.task_id), {include_blank: true}, {required: 'true', class: 'form-control' , placeholder: 'Select task'} %>
  </div>

  <div class="field form-group">
    <%= form.label :comment %>
    <%= form.text_field :comment, class: 'form-control', placeholder: 'comment', required: true %>
  </div>

  <div class="actions form-group">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
