<%= form_with(model: comment_file, local: true) do |form| %>
  <% if comment_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_file.errors.count, "error") %> prohibited this comment_file from being saved:</h2>

      <ul>
      <% comment_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment_id %>
    <%= form.text_field :comment_id %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.text_field :file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
